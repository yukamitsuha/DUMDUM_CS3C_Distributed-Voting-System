import requests

SUPABASE_URL = "https://gecnjqonhfmrtmgfvnep.supabase.co/rest/v1"
SUPABASE_ANON_KEY = "sb_publishable_jl9e1u-fkPZLFFqTTfeQVg_dbiwhngu"

headers = {
    "apikey": SUPABASE_ANON_KEY,
    "Authorization": f"Bearer {SUPABASE_ANON_KEY}"
}

response = requests.get(f"{SUPABASE_URL}/votes?select=choice", headers=headers)
votes = response.json()

if votes:
    from collections import Counter
    choices = [v['choice'] for v in votes]
    count = Counter(choices)
    
    print(f"Total Votes: {len(votes)}")
    print("\nVotes by Choice:")
    for choice, num in count.items():
        print(f"Choice {choice}: {num}")
else:
    print("No votes found.")
